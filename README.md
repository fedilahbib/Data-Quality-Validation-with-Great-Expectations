# Data Quality Validation with Great Expectations

This project is a practical example from a data knowledge session, demonstrating how to build a robust data quality validation pipeline using Great Expectations.

## Install


```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt

```

##  Run validation
```bash
python run_validation.py

```